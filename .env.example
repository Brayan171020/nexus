# PostgreSQL / Neon connection string (replace with a local or managed database URL)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
PORT=3000
NODE_ENV=development
REDIS_HOST=localhost
REDIS_PORT=6379
# Optional provider settings for a future AI adapter
AI_PROVIDER=mock
AI_MODEL=triage-v1
